import React from "react";
import styled from "styled-components";

const Grid = (props) => {
  const {} = props;

  const styles = {};
  return (
    null
  );
};

Grid.defaultProps = {
 
  _onClick: () => {},
};


export default Grid;
